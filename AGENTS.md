# AGENTS.md

## Agent roster (cross-repo)

> 편성표 SoT: yohan-brain `memory/core/agent-roster.yaml` (status=active면 obey).
> 계약: yohan-brain `memory/core/ecosystem-contract.yaml` · tier: `inheritance-registry.yaml`.

- Orca = 지휘소(ADE). 메인 CLI·모델·effort는 roster + 지휘자가 고른다.
- 같은 레포·같은 브랜치에 에이전트 2명 금지 → worktree만.
- Antigravity(`agy`) = 보조·검증 필수. Orca inject 비지원(수동 send).
- 배포·시크릿·npm publish = 사람 Gate.
